namespace v1.Repository
{
    public static class RepositoryExtensions
    {
        // public static List<T> ToList<T>(this IRepository repository, string query)
        // {
        //     return repository.ExecuteQuery<List<T>>(query);
        // }

        // public static T? Single<T>(this IRepository repository, string query)
        // {
        //     var resultList = repository.ExecuteQuery<List<T>>(query);
        //     return resultList.Count > 0 ? resultList[0] : default(T);
        // }
    }
}
